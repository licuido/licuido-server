//Do Not Delete this file
import { Sequelize } from "sequelize";

/** 
 * This establishes the other custom defined relationships
 * which are beyond the scope of auto generation
 * @param sequelize Sequelize
*/
export function establishCustomRelations(sequelize: Sequelize) {
 //Example 
 /* modelName1.belongsToMany(modeName2, {
    through: modelName3,
    foreignKey: columnName1,
    otherKey: columnName2,
  }); */
}
